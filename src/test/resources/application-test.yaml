spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/semosan
    username: pooreum
    password: pooreum02
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
  data:
    redis:
      host: localhost
      port: 6379

firebase:
  service-account-path: dummy-firebase-service-account.json

jwt:
  secret: do-not-use-test-only-jwt-secret-00000000000000000000000000000000
  access-token-expiration: 3600000
  refresh-token-expiration: 604800000

kakao:
  client-id: dummy
  redirect-uri: http://localhost:8080/oauth/kakao/callback
  client-secret: dummy
  admin-key: dummy

apple:
  client-id: dummy

tracking:
  stream-key: tracking:gps
  consumer-group: tracking-consumer-group
  stream-max-len: 100000

test:
  secret-key: test-only-secret-key

minio:
  endpoint: http://localhost:9000
  access-key: minio-admin
  secret-key: minio-secret-password
  public-url: http://localhost:9000

discord:
  alert:
    enabled: false
    webhook-url: https://discord.example.com/webhook
    report-webhook-url: https://discord.example.com/report

grafana:
  base-url: http://grafana.example.com
  loki:
    datasource-uid: loki
    namespace: test
    app: test-api

weather:
  api-key: dummy
